# Решение проблемы с отсутствующими DLL файлами Qt6

## Проблема
При запуске скомпилированного приложения возникали ошибки:
- Не удается продолжить выполнение кода, поскольку система не обнаружила Qt6Widgetsd.dll
- Не удается продолжить выполнение кода, поскольку система не обнаружила Qt6Cored.dll  
- Не удается продолжить выполнение кода, поскольку система не обнаружила Qt6Gui.dll

## Причина
Проект был собран в отладочной конфигурации (Debug), но в системе отсутствуют отладочные версии DLL файлов Qt6. В vcpkg установлены только релизные версии Qt6 библиотек.

## Решение

### ✅ Работающее решение: Release версия
Release версия приложения работает корректно и запускается без ошибок.

**Для запуска приложения используйте:**
```bash
cd build/release
SolarSimMVP.exe
```

### 🔧 Автоматическое развертывание зависимостей
Для Release версии было выполнено автоматическое развертывание всех необходимых зависимостей с помощью `windeployqt6.exe`:

```bash
vcpkg_installed/x64-windows/tools/Qt6/bin/windeployqt6.exe build/release/SolarSimMVP.exe
```

Эта команда автоматически скопировала в папку `build/release/`:
- Все необходимые DLL файлы Qt6
- Плагины Qt (платформы, форматы изображений, стили)
- Дополнительные зависимости (ICU, OpenSSL, и др.)

### ⚠️ Debug версия
Debug версия требует отладочные DLL файлы (Qt6Widgetsd.dll, Qt6Cored.dll, Qt6Guid.dll), которые отсутствуют в текущей установке vcpkg. 

**Рекомендации для Debug версии:**
1. Используйте Release версию для разработки и тестирования
2. При необходимости отладки используйте Visual Studio Debugger с Release сборкой
3. Для полноценной Debug сборки потребуется установка отладочных версий Qt6 через vcpkg

## Структура проекта
- `build/release/SolarSimMVP.exe` - рабочая версия приложения со всеми зависимостями
- `build/debug/SolarSimMVP.exe` - отладочная версия (требует отладочные DLL)
- `vcpkg_installed/` - установленные библиотеки через vcpkg

## Вывод
**Проблема решена!** Release версия приложения успешно запускается и работает корректно со всеми необходимыми зависимостями Qt6.