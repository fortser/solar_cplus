# Руководство по участию в проекте

Спасибо за ваш интерес к участию в развитии Solar Orbital Simulator! Этот документ содержит основные принципы и рекомендации для внесения вклада в проект.

## 🚀 Способы участия

Мы приветствуем различные виды вклада:

- 🐛 **Сообщения об ошибках**: Найденные баги и проблемы
- 💡 **Предложения функций**: Идеи по улучшению проекта
- 🔧 **Исправления**: Pull requests с исправлениями
- 📚 **Документация**: Улучшение README и других документов
- 🎨 **UI/UX**: Улучшения интерфейса пользователя
- 🧪 **Тестирование**: Новые тесты и улучшение покрытия

## 🛠 Разработка

### Предварительные требования

- C++17 или выше
- Qt 6.x
- CMake 3.16+
- Visual Studio 2019/2022 (Windows) или аналогичная среда
- vcpkg для управления зависимостями

### Настройка среды разработки

1. **Клонирование репозитория**:
```bash
git clone https://github.com/your-username/solar-orbital-simulator.git
cd solar-orbital-simulator
```

2. **Инициализация зависимостей**:
```bash
vcpkg install
```

3. **Сборка проекта**:
```bash
cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake
cmake --build build --config Release
```

4. **Запуск тестов**:
```bash
cd build
ctest --output-on-failure
```

## 📝 Стандарты кода

### C++ Style Guide

- Используйте современный C++ (C++17+)
- Предпочитайте `auto` для вывода типов
- Используйте `constexpr` для констант времени компиляции
- Избегайте raw указателей, предпочитайте умные указатели
- Следуйте принципу RAII (Resource Acquisition Is Initialization)

### Примеры кода

**Хорошо:**
```cpp
// Используйте современные возможности C++
auto bodies = std::vector<CelestialBody>{};
constexpr double G = 6.67430e-11; // Гравитационная постоянная

// Предпочитайте умные указатели
auto physicsEngine = std::make_unique<PhysicsEngine>();
```

**Плохо:**
```cpp
// Избегайте устаревших практик
std::vector<CelestialBody> *bodies = new std::vector<CelestialBody>();
double G = 6.67430e-11; // Не constexpr
PhysicsEngine *engine = new PhysicsEngine(); // Raw pointer
```

### Именование

- **Файлы**: `PascalCase` для классов, `snake_case` для остального
- **Классы**: `PascalCase` (например, `PhysicsEngine`)
- **Функции и переменные**: `camelCase` (например, `updateSimulation()`)
- **Константы**: `UPPER_SNAKE_CASE` (например, `MAX_VELOCITY`)
- **Приватные члены**: `m_` префикс (например, `m_bodies`)

### Документация

- Используйте Doxygen-совместимые комментарии
- Документируйте все публичные интерфейсы
- Включайте примеры использования в документацию

Пример:
```cpp
/**
 * @brief Выполняет один шаг физической симуляции
 * @param dt Временной шаг в секундах
 * @note Использует метод Velocity Verlet для численного интегрирования
 */
void step(double dt);
```

## 🧪 Тестирование

### Модульные тесты

Все новые функции должны сопровождаться тестами:

- Используйте Google Test или аналогичный фреймворк
- Тестируйте граничные случаи и ошибки
- Стремитесь к покрытию не менее 80%

Пример теста:
```cpp
TEST(PhysicsEngineTest, VelocityVerletIntegration) {
    PhysicsEngine engine;
    CelestialBody sun("Sun", 1.989e30, 696340000, Qt::yellow, {0, 0}, {0, 0});
    engine.addBody(sun);
    
    engine.step(3600); // 1 час
    
    // Проверяем что позиция не изменилась (неподвижное тело)
    EXPECT_EQ(engine.bodies[0].position.x(), 0.0);
    EXPECT_EQ(engine.bodies[0].position.y(), 0.0);
}
```

### Тестирование производительности

Для функций, критичных к производительности:
- Включайте бенчмарки
- Мониторьте использование памяти
- Проверяйте стабильность численных расчетов

## 📋 Процесс Pull Request

### 1. Создание ветки

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

### 2. Разработка

- Следуйте стандартам кода проекта
- Добавляйте тесты для новой функциональности
- Обновляйте документацию при необходимости
- Убедитесь что все тесты проходят

### 3. Commit сообщения

Используйте conventional commits:

```
feat: добавить поддержку Марса в симуляцию
fix: исправить утечку памяти в PhysicsEngine
docs: обновить инструкции по сборке
test: добавить тесты для Velocity Verlet
refactor: упростить структуру CelestialBody
```

### 4. Pull Request

Создайте Pull Request с:

- **Четким заголовком** описывающим изменения
- **Подробным описанием** что было изменено и почему
- **Ссылкой на связанную Issue** (если есть)
- **Скриншотами** для UI изменений
- **Списком изменений** в коде

Шаблон Pull Request:
```markdown
## Описание
Краткое описание изменений

## Изменения
- [ ] Добавлена новая функция X
- [ ] Исправлен баг Y
- [ ] Улучшена производительность Z

## Тестирование
- [ ] Добавлены модульные тесты
- [ ] Проведено ручное тестирование
- [ ] Проверена совместимость с разными ОС

## Скриншоты (если применимо)
```

## 🐛 Сообщения об ошибках

При сообщении об ошибке включайте:

1. **Описание проблемы**: Что происходит
2. **Шаги для воспроизведения**: Пошаговые инструкции
3. **Ожидаемое поведение**: Что должно происходить
4. **Фактическое поведение**: Что происходит на самом деле
5. **Скриншоты/видео**: Если применимо
6. **Информация о системе**: OS, версия Qt, компилятор

### Шаблон Issue

```markdown
**Описание ошибки**
Четкое описание проблемы

**Шаги для воспроизведения**
1. Запустите '...'
2. Нажмите '...'
3. Прокрутите до '...'
4. Увидите ошибку

**Ожидаемое поведение**
Что вы ожидали увидеть

**Фактическое поведение**
Что произошло на самом деле

**Скриншоты**
Если применимо, добавьте скриншоты

**Информация о системе**
- OS: [e.g. Windows 10]
- Qt версия: [e.g. 6.4.0]
- Компилятор: [e.g. MSVC 2019]
```

## 💡 Предложения функций

При предложении новой функции:

1. **Проверьте существующие Issues**: Возможно, функция уже предложена
2. **Опишите проблему**: Какую проблему решает функция
3. **Предложите решение**: Как функция должна работать
4. **Обоснуйте необходимость**: Почему это важно для проекта

## 📚 Документация

Улучшения документации всегда приветствуются:

- Исправление опечаток
- Улучшение ясности
- Добавление примеров
- Обновление устаревшей информации
- Перевод на другие языки

## 🏷 Версионирование

Проект использует [Semantic Versioning](https://semver.org/):

- **MAJOR**: Несовместимые изменения API
- **MINOR**: Новые функции, совместимые с предыдущими
- **PATCH**: Исправления ошибок, совместимые с предыдущими

## 🎯 Приоритеты разработки

Текущие приоритеты проекта:

1. **Стабильность**: Устранение критических ошибок
2. **Расширение функций**: Добавление планет и объектов
3. **Производительность**: Оптимизация расчетов
4. **UI/UX**: Улучшение пользовательского интерфейса
5. **Документация**: Улучшение документации и примеров

## ❓ Вопросы

Если у вас есть вопросы:

1. Проверьте [Issues](https://github.com/your-username/solar-orbital-simulator/issues)
2. Создайте новую Issue с меткой `question`
3. Присоединяйтесь к обсуждениям

## 🙏 Благодарности

Всех контрибьюторов мы будем:

- Добавлять в файл `CONTRIBUTORS.md`
- Упоминать в релизах
- Благодарить в документации

---

**Спабо за ваш вклад в развитие Solar Orbital Simulator!** 🚀